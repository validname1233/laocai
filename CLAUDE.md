## Agent skills

### Issue tracker

本仓库的 issue 和 spec 使用 GitHub Issues 管理；相关操作使用 `gh` CLI。详见 `docs/agents/issue-tracker.md`。

### Triage labels

本仓库使用 triage skill 的默认标签：`needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix`。详见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用 multi-context 布局；上下文索引位于根目录 `CONTEXT-MAP.md`，各上下文的领域术语位于对应的 `CONTEXT.md`，架构决策位于 `docs/adr/` 或对应上下文的 ADR 目录。详见 `docs/agents/domain.md`。
